pipeline {
    agent any
    stages {
        stage("Compile") {
            steps {
                echo "compile..."
            }
        }
        stage("Unit test") {
            steps {
                echo "unit test..."
            }
        }
       stage("Deployment") {
            steps {
                echo "deployment..."
            }
       }
    }
}

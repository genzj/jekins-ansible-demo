pipeline {
    agent any
    stages {
        stage("Compile") {
            steps {
                sh “echo compile...”
            }
        }
        stage("Unit test") {
            steps {
                sh “echo unit test...”
            }
        }
       stage("Deployment") {
            steps {
                sh “echo deployment...”
            }
       }
    }
}
